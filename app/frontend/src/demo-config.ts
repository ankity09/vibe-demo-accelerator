import type { DemoConfig } from '@/types'

export const demoConfig: DemoConfig = {
  name: 'VDA Demo',
  customer: 'Demo Customer',
  industry: 'General',
  features: [],
  layout: 'sidebar',
  theme: 'neutral',
  mode: 'dark',
}
